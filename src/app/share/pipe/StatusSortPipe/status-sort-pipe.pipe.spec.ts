import { StatusSortPipePipe } from './status-sort-pipe.pipe';

describe('StatusSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
